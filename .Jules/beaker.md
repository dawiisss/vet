## 2024-06-15 - [useFocusTrap Test Environment] Learning: [When testing React hooks that interact with the DOM using Jest, ensure the test file includes the `/** @jest-environment jsdom */` pragma if jsdom is not the default environment, to avoid 'document is not defined' errors.] Action: [Always check the environment configuration or explicitly declare `jsdom` when writing DOM-dependent tests.]
## 2024-06-15 - [useFocusTrap Lint Error Fix] Learning: [In React test files, ensure inline wrapper components (like `TestComponent`) have explicitly typed props (e.g., `({ active = true }: { active?: boolean })`) to satisfy the `react/prop-types` ESLint rule and prevent CI linting failures.] Action: [Always type props for local test components.]
